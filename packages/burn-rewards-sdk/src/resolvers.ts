import { combineResolvers } from "@render-foundation/spl-utils";
import { circuitBreakerResolvers } from "@render-foundation/circuit-breaker-sdk";

export const burnRewardsResolvers = combineResolvers(
);
